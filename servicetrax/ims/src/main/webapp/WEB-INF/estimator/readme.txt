template.xls and template2.xls are the old versions.  They don't work with Apache POI.

template.xlsm and template2.xlsm are the new versions.  They work with Apache POI.

template_http.xlsm is the version of template.xlsm that uses http rather than https.  Use this for testing from a DEV
environment.

The genius who decided it was a good idea to hardcode the URL prefix in the macro should be flogged...